function createElement(tag, text, attrs) {
    var originAttrs = ['className', 'id', 'src', 'checked', 'href'];
    var element = document.createElement(tag);
    
}


function DxTree(wrap, data, opts) {

    this.nodeList ={};
    this.wrap = wrap;
    this.data = data;


}

